# Public, non-secret local configuration only.
PUBLIC_API_BASE_PATH=/api
PUBLIC_APP_ENV=local

# Real secrets are configured only as Cloudflare Secrets (wrangler secret put).
# Never put secret values in this file, Cloudflare vars, git, or client-side code.
