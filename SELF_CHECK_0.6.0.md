# SELF CHECK 0.6.0-dev

## Проверено перед выдачей

- Архив упакован с корневой папкой `TSB-Hub-v0.6.0-dev/`.
- В корне проекта есть `index.html` для GitHub Pages.
- Версия обновлена до `0.6.0-dev` в HTML/JS/service-worker/README.
- CSS/JS подключаются с параметром `?v=0.6.0-dev`.
- Добавлен `.nojekyll` для GitHub Pages.
- Основное ядро 0.5.8 не переписано с нуля.
- Изменения 0.6.0 касаются в первую очередь адаптивного интерфейса.
- `node --check js/app.js` проходит без синтаксических ошибок.

## Что тестировать пользователю

1. Загрузка через GitHub Pages на ПК.
2. Загрузка через GitHub Pages на Android/Chrome.
3. Вкладки внизу на телефоне.
4. Экран «Сегодня» на узком экране.
5. Планы, питание, важные даты, настройки и синхронизация.
6. Импорт ранее экспортированного `tsb_data.json`.
