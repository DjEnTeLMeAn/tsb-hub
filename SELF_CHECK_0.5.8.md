# SELF CHECK 0.5.8-dev

Проверено перед выдачей:

- `node --check js/app.js` проходит без синтаксических ошибок.
- В `app.js` добавлены реальные функции `exportData()` и `importData()`, которые использует вкладка синхронизации.
- `renderSync()` больше не должен падать из-за отсутствующих функций экспорта/импорта.
- Кнопка очистки базы остаётся защищённой вводом слова `подтверждаю`.
- Версия обновлена в HTML/CSS/JS до `0.5.8-dev`.

Что проверить вручную:

1. Настройки → диагностика не должна показывать `sync`.
2. Синхронизация → экспорт, импорт, копирование JSON, очистка базы.
3. Очистка базы должна открывать окно с вводом `подтверждаю`, а не срабатывать сразу.
