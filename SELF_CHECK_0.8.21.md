# Самопроверка TSB Hub · релиз `0.13.3-finance-transaction-control-20260808`

Файл сохранён под историческим именем `SELF_CHECK_0.8.21.md`; контрольная версия берётся из `version.json`.

- [x] Версия синхронизирована с `version.json` во всех release-маркерах HTML, manifest, service worker и JS-ссылках.
- [x] Структура проекта сохранена; обязательные файлы и обе PWA-иконки присутствуют.
- [x] `node --check js/app.js` проходит без синтаксических ошибок.
- [x] localStorage-ключ не менялся.
- [x] Backend, облако, зависимости и сборщик не добавлялись.
- [x] Полный backup/import и Finance JSON/CSV разделены и имеют разные контракты.
- [x] ZIP должен содержать корневую папку проекта.

## Проверить вручную

1. Открытие приложения через `index.html` и загрузка offline shell.
2. Переключение вкладок через кнопку-книжку.
3. Ползунки ежедневного отчёта: подписи 0/25/50/75/100 стоят ровнее под положениями бегунка.
4. Ежедневный отчёт сохраняется.
5. Блок «Незавершённое за прошлые дни» показывает кнопки «Выполнено», «Перенести», «...». 
6. «Выполнено» у пропущенной задачи открывает выбор «В тот день» / «Сегодня».
7. «Перенести» работает как раньше.
8. В меню «...» работают «Открыть день» и «Скрыть».
9. Задачи, питание, финансы, полный backup/import и Finance JSON/CSV работают как раньше.
10. PWA обновляет service worker после загрузки на GitHub Pages; release и cache совпадают с `version.json`.

- [x] Проверены правки финансов: отрицательный баланс сохраняет знак, плановые поступления и обязательные расходы снова сворачиваются.
- [x] Диалог выполнения пропущенной задачи очищен от лишнего пояснительного текста.
