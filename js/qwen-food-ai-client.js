(function (global) {
  'use strict';
  const ORIGIN = 'https://dashscope-intl.aliyuncs.com';
  const ENDPOINT = ORIGIN + '/compatible-mode/v1/chat/completions';
  const DEFAULT_MODEL = 'qwen3.8-flash';
  const MAX_SOURCE = 10 * 1024 * 1024;
  const MAX_REQUEST = 6 * 1024 * 1024, MAX_RESPONSE = 512 * 1024, MAX_DIMENSION = 1280, MAX_PREPARED = 3 * 1024 * 1024, JPEG_QUALITY = 0.78;
  const TYPES = new Set(['image/jpeg', 'image/png', 'image/webp']);
  const FIELDS = ['name','ingredients','portionGrams','volumeMl','calories','protein','fat','carbs','comment','confidence'];
  function fail(code, message) { const e = new Error(message); e.code = code; return e; }
  function text(v, max, required) { if (typeof v !== 'string' || v.length > max || (required && !v.trim())) throw fail('INVALID_RESPONSE','Некорректный текст.'); return v.trim(); }
  function num(v, max) { if (typeof v !== 'number' || !Number.isFinite(v) || v < 0 || v > max) throw fail('INVALID_RESPONSE','Некорректное число.'); return v; }
  function closeBitmap(b) { try { b?.close?.(); } catch (_) {} }
  function readBlob(blob) { return new Promise((resolve,reject)=>{ if(typeof global.FileReader!=='function') return reject(fail('INVALID_IMAGE','Чтение фото недоступно.')); const r=new global.FileReader(); r.onerror=()=>reject(fail('INVALID_IMAGE','Не удалось прочитать фото.')); r.onload=()=>{const s=String(r.result||''), p='data:image/jpeg;base64,'; const v=s.startsWith(p)?s.slice(p.length):''; if(!v||!/^[A-Za-z0-9+/]+={0,2}$/.test(v)||Math.floor(v.length*3/4)>MAX_PREPARED) return reject(fail(v?'IMAGE_TOO_LARGE':'INVALID_IMAGE','Некорректное изображение.')); resolve(v);}; r.readAsDataURL(blob); }); }
  async function prepare(file) {
    if (!(typeof global.Blob==='function' && file instanceof global.Blob) || !TYPES.has(file.type)) throw fail('INVALID_IMAGE','Выберите JPEG, PNG или WebP.');
    if (!Number.isSafeInteger(file.size) || file.size<=0) throw fail('INVALID_IMAGE','Некорректный размер фото.');
    if (file.size>MAX_SOURCE) throw fail('IMAGE_TOO_LARGE','Исходное фото слишком велико.');
    if(typeof global.createImageBitmap!=='function'||!global.document?.createElement) throw fail('INVALID_IMAGE','Обработка фото недоступна.');
    let b; try { b=await global.createImageBitmap(file); if(!Number.isFinite(b.width)||!Number.isFinite(b.height)||b.width<=0||b.height<=0) throw fail('INVALID_IMAGE','Некорректные размеры фото.'); const scale=Math.min(1,MAX_DIMENSION/Math.max(b.width,b.height)), c=global.document.createElement('canvas'); c.width=Math.max(1,Math.round(b.width*scale)); c.height=Math.max(1,Math.round(b.height*scale)); const x=c.getContext('2d'); if(!x||typeof c.toBlob!=='function') throw fail('INVALID_IMAGE','Обработка фото недоступна.'); x.drawImage(b,0,0,c.width,c.height); const out=await new Promise((res,rej)=>{try{c.toBlob(z=>!z?rej(fail('INVALID_IMAGE','Не удалось подготовить фото.')):z.size>MAX_PREPARED?rej(fail('IMAGE_TOO_LARGE','Подготовленное фото слишком велико.')):res(z),'image/jpeg',JPEG_QUALITY);}catch(_){rej(fail('INVALID_IMAGE','Не удалось подготовить фото.'));}}); return {mimeType:'image/jpeg',data:await readBlob(out)}; } catch(e) { if(e?.code) throw e; throw fail('INVALID_IMAGE','Не удалось декодировать фото.'); } finally { closeBitmap(b); }
  }
  function normalize(v) { if(!v||typeof v!=='object'||Array.isArray(v)) throw fail('INVALID_RESPONSE','Неверный ответ модели.'); const keys=Object.keys(v); if(keys.length!==FIELDS.length||keys.some(k=>!FIELDS.includes(k))||FIELDS.some(k=>!Object.hasOwn(v,k))) throw fail('INVALID_RESPONSE','Ответ не соответствует схеме.'); if(!Array.isArray(v.ingredients)||v.ingredients.length>30) throw fail('INVALID_RESPONSE','Некорректные ингредиенты.'); return {name:text(v.name,160,true),ingredients:v.ingredients.map(x=>text(x,120,true)),portionGrams:v.portionGrams===null?null:num(v.portionGrams,20000),volumeMl:v.volumeMl===null?null:num(v.volumeMl,20000),calories:num(v.calories,20000),protein:num(v.protein,5000),fat:num(v.fat,5000),carbs:num(v.carbs,5000),comment:text(v.comment,1000),confidence:v.confidence===null?null:num(v.confidence,1)}; }
  async function readResponse(r) { const l=r.headers?.get?.('content-length'); if(l!==null&&l!==undefined&&l!==''&&(!/^\d+$/.test(l)||Number(l)>MAX_RESPONSE)) throw fail('INVALID_RESPONSE','Ответ слишком велик.'); if(!r.body?.getReader){if(!/^\d+$/.test(l||'')) throw fail('INVALID_RESPONSE','Размер ответа неизвестен.'); const s=await r.text(); if(new global.Blob([s]).size>MAX_RESPONSE) throw fail('INVALID_RESPONSE','Ответ слишком велик.'); return s;} const rd=r.body.getReader(), d=new global.TextDecoder('utf-8',{fatal:true}); let n=0,s=''; try{for(;;){const q=await rd.read();if(q.done)break;n+=q.value.byteLength;if(n>MAX_RESPONSE){try{await rd.cancel();}catch(_){} throw fail('INVALID_RESPONSE','Ответ слишком велик.');}s+=d.decode(q.value,{stream:true});}return s+d.decode();}catch(e){if(e?.code)throw e;throw fail('INVALID_RESPONSE','Ответ повреждён.');}finally{try{rd.releaseLock?.();}catch(_){} } }
  function parse(raw) { let e; try{e=JSON.parse(raw);}catch(_){throw fail('INVALID_RESPONSE','Сервис вернул не JSON.');} const c=e?.choices; if(!Array.isArray(c)||c.length!==1||c[0]?.finish_reason!=='stop'||c[0]?.message?.role!=='assistant'||typeof c[0]?.message?.content!=='string'||c[0].message.content.length>MAX_RESPONSE) throw fail('INVALID_RESPONSE','Ответ пуст или неоднозначен.'); const s=c[0].message.content.trim(); if(s.startsWith('```')||s.includes('```')) throw fail('INVALID_RESPONSE','Модель вернула не JSON.'); try{return normalize(JSON.parse(s));}catch(x){if(x?.code)throw x;throw fail('INVALID_RESPONSE','Модель вернула не JSON.');} }
  function body(image) { return {model:DEFAULT_MODEL,messages:[{role:'user',content:[{type:'image_url',image_url:{url:'data:image/jpeg;base64,'+image.data}},{type:'text',text:'Проанализируй только видимую еду и всю порцию. Не выдумывай скрытые продукты. Верни только JSON ровно с ключами name, ingredients, portionGrams, volumeMl, calories, protein, fat, carbs, comment, confidence; без markdown, пояснений, массивов кандидатов и дополнительных ключей.'}]}],response_format:{type:'json_object'},stream:false}; }
  async function analyzeFoodPhoto(o={}) { const k=o.apiKey; if(typeof k!=='string'||k.length<8||k.length>4096||!/^[\x21-\x7e]+$/.test(k)) throw fail('NO_KEY','API-ключ не указан.'); if(o.model!==undefined&&o.model!==DEFAULT_MODEL) throw fail('API','Некорректная модель.'); if(o.signal?.aborted) throw fail('ABORTED','Запрос отменён.'); const im=await prepare(o.file); if(o.signal?.aborted) throw fail('ABORTED','Запрос отменён.'); const b=JSON.stringify(body(im)); if(new global.Blob([b]).size>MAX_REQUEST) throw fail('IMAGE_TOO_LARGE','Запрос слишком велик.'); if(typeof global.fetch!=='function') throw fail('NETWORK','Сеть недоступна.'); const ac=new global.AbortController(); let timed=false; const abort=()=>ac.abort(); o.signal?.addEventListener('abort',abort,{once:true}); const timer=global.setTimeout(()=>{timed=true;ac.abort();},20000); try{const r=await global.fetch(ENDPOINT,{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+k},cache:'no-store',referrerPolicy:'no-referrer',body:b,signal:ac.signal}); if(!r?.ok) throw fail(r.status===401||r.status===403?'AUTH':r.status===429?'QUOTA':'API','Ошибка сервиса анализа.'); const ct=r.headers?.get?.('content-type')||''; if(ct.split(';')[0].trim().toLowerCase()!=='application/json') throw fail('API','Неожиданный формат ответа.'); return parse(await readResponse(r));}catch(e){if(e?.name==='AbortError')throw fail(timed?'TIMEOUT':'ABORTED',timed?'Время ожидания истекло.':'Запрос отменён.');if(e?.code)throw e;throw fail('NETWORK','Не удалось связаться с сервисом.');}finally{global.clearTimeout(timer);o.signal?.removeEventListener('abort',abort);} }
  global.TSBQwenFoodAIClient=Object.freeze({analyzeFoodPhoto});
})(window);
